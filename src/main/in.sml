class Main {
    def main() : int {
        writeln(new c().func(3));
        return x;
    }
}

class d {
    var arr: int[];
    def b() : String {
        arr = new int[4];
        return arr.length;
    }
}

class d extends d {
    var e: String;
    def func(sd: int): int {
        e = (1 + 2 * 3 + 5) / 6;
        writeln(e);
        if (e <> 1) then
            while (e <> 5) {
                e = e + 1;
            }
        else
            e = 5;
        return -e - this.b() + sd;
    }
}